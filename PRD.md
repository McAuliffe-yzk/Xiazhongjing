# 匣中镜 PRD v2.0 · Community Beta

日期：2026-08-15

## 1. 产品定义

匣中镜是面向单个内容创作者的本地优先 Personal Agent。它帮助用户把自己的历史文稿蒸馏为可审核的个人 DNA，把真实素材发展为第一人称 Vlog 文案，并用个人精神书库、镜中人和书中人持续积累创作上下文。

## 2. v0.3.0-beta 目标

新用户无需维护者的任何示例数据，即可完成：

1. 配置并真实验证文本模型。
2. 导入自己的历史文案，审核和发布个人 DNA。
3. 创建自己的精神书库，导入书籍原文或阅读笔记。
4. 只把人工确认的直接引文用于文案生成。
5. 使用个人 DNA 与项目所选书籍完成第一篇创作。
6. 让镜中人基于个人历史文稿回答，并从书库动态创建书中人。

## 3. 核心边界

- 单用户、本地优先，不提供云端多租户账户系统。
- 历史文稿、项目、对话、书籍资料、图片和密钥不进入公开仓库。
- 镜中人与书中人不绑定创作项目，也不会自动修改项目素材。
- 重写当前稿时只使用编辑器当前文本与个人 DNA，不回看已被用户修改过的原始素材。
- 通用书籍中的无引号短句默认待审核，模型常识不能被包装成直接引文。

## 4. 生成契约

- 主链路：个人风格撰写、抖音 Vlog 优化、书库金句植入。
- 叙事模式：默认、排比递进、六段式、先抑后扬。
- 表达边界：原句、改写（默认）、阐释。
- 手动字数是硬约束；自动字数根据信息密度决定。
- 书库金句目标数量由策略控制，没有合适时机可以少用或不用。

## 5. 技术架构

产品采用模块化单体：FastAPI 路由层、应用用例层、领域服务、版本化 Skills、原生 Web 前端与 SQLite 本地持久化。Creator Onboarding 只聚合真实就绪状态，不复制或改写用户数据。

## 6. 发布标准

- 空白安装不包含维护者身份、项目、书籍、人物、历史文稿、数据库、资产或 API Key。
- 解压后的公开源码可以通过全量自动化测试、Python 编译和前端 JavaScript 语法检查。
- macOS、Windows、Linux 至少具备可重复的本地安装路径。
- 公开 GitHub 使用脱敏包建立干净历史，不公开私人工作区旧提交。
